export const standaloneEnigmaPieceStyles = "function makePieceStyle(imageData, rows, cols, pieceIndex, rotation = 0) {\n  const safeRows = Math.max(1, Math.round(Number(rows) || 1));\n  const safeCols = Math.max(1, Math.round(Number(cols) || 1));\n  const safePieceIndex = Math.max(0, Number(pieceIndex) || 0);\n  const row = Math.floor(safePieceIndex / safeCols);\n  const col = safePieceIndex % safeCols;\n  return [\n    'background-image:' + cssMediaUrl(imageData, 'image'),\n    'background-size:' + cssNumber(safeCols * 100, 100, 1, 10000) + '% ' + cssNumber(safeRows * 100, 100, 1, 10000) + '%',\n    'background-position:' + safeStylePercent(safeCols === 1 ? 0 : (col / (safeCols - 1)) * 100, 0) + ' ' + safeStylePercent(safeRows === 1 ? 0 : (row / (safeRows - 1)) * 100, 0),\n    'transform:rotate(' + cssNumber(rotation, 0, -3600, 3600) + 'deg)',\n  ].join(';');\n}\n\n";

export const standaloneEnigmaPlayback = "function clearSimonPlayback() {\n  simonTimeouts.forEach((timeoutId) => clearTimeout(timeoutId));\n  simonTimeouts = [];\n  state.simonPlaybackIndex = -1;\n}\n\nfunction startSimonPlayback(enigma) {\n  clearSimonPlayback();\n  state.simonPlayerTurn = false;\n  state.enigmaColorAttempt = [];\n  const sequence = enigma.solutionColors || [];\n  sequence.forEach((color, index) => {\n    const showId = window.setTimeout(() => {\n      state.simonPlaybackIndex = index;\n      render();\n    }, index * 800 + 250);\n    const hideId = window.setTimeout(() => {\n      state.simonPlaybackIndex = -1;\n      render();\n    }, index * 800 + 700);\n    simonTimeouts.push(showId, hideId);\n  });\n  const endId = window.setTimeout(() => {\n    state.simonPlaybackIndex = -1;\n    state.simonPlayerTurn = true;\n    render();\n  }, sequence.length * 800 + 750);\n  simonTimeouts.push(endId);\n}\n\n";

export const standaloneEnigmaActions = "function applyEnigmaSuccess(enigma, hotspot) {\n  if (hotspot && enigma.unlockType !== 'none') {\n    applyHotspotSideEffects(hotspot);\n  }\n  if (enigma.successMessage) state.dialogue = enigma.successMessage;\n\n  if (enigma.unlockType === 'scene' && enigma.targetSceneId) {\ngoToScene(enigma.targetSceneId, enigma.successMessage || 'Nouvelle scène débloquée.');\n  } else if (enigma.unlockType === 'cinematic' && enigma.targetCinematicId) {\n    launchCinematic(enigma.targetCinematicId);\n  } else if (hotspot) {\n    applyHotspotAction(hotspot);\n  }\n}\n\nfunction closeEnigma() {\n  clearSimonPlayback();\n  state.activeEnigma = null;\n  state.enigmaCodeInput = '';\n  state.enigmaColorAttempt = [];\n  state.enigmaPuzzleOrder = [];\n  state.enigmaPuzzleSelectedIndex = null;\n  state.enigmaDragBank = [];\n  state.enigmaDragSlots = [];\n  state.enigmaDraggedPiece = null;\n  state.enigmaRotationAngles = [];\n  state.simonPlayerTurn = false;\n}\n\nfunction solveActiveEnigma() {\n  if (!state.activeEnigma?.enigma) return;\n  const { enigma, hotspot } = state.activeEnigma;\n  if (!state.solvedEnigmaIds.includes(enigma.id)) {\n    state.solvedEnigmaIds = [...state.solvedEnigmaIds, enigma.id];\n  }\n  closeEnigma();\n  applyEnigmaSuccess(enigma, hotspot);\n  render();\n}\n\nfunction failActiveEnigma() {\n  if (!state.activeEnigma?.enigma) return;\n  state.dialogue = state.activeEnigma.enigma.failMessage || 'Ce n’est pas la bonne réponse.';\n}\n\nfunction openEnigma(enigma, hotspot = null) {\n  const pieceCount = Math.max(4, (Number(enigma.gridRows) || 3) * (Number(enigma.gridCols) || 3));\n  state.activeEnigma = { enigma, hotspot };\n  state.enigmaCodeInput = '';\n  state.enigmaColorAttempt = [];\n  state.enigmaPuzzleSelectedIndex = null;\n  state.enigmaDraggedPiece = null;\n  state.simonPlayerTurn = enigma.type !== 'simon';\n\n  if (enigma.type === 'puzzle') {\n    state.enigmaPuzzleOrder = shuffledIndices(pieceCount);\n  } else {\n    state.enigmaPuzzleOrder = [];\n  }\n\n  if (enigma.type === 'dragdrop') {\n    state.enigmaDragBank = shuffledIndices(pieceCount);\n    state.enigmaDragSlots = Array.from({ length: pieceCount }, () => null);\n  } else {\n    state.enigmaDragBank = [];\n    state.enigmaDragSlots = [];\n  }\n\n  if (enigma.type === 'rotation') {\n    state.enigmaRotationAngles = randomRotations(pieceCount);\n  } else {\n    state.enigmaRotationAngles = [];\n  }\n\n  if (enigma.type === 'simon') {\n    startSimonPlayback(enigma);\n  } else {\n    clearSimonPlayback();\n  }\n}\n\nfunction getActiveEnigmaAnswer() {\n  return {\n    codeInput: state.enigmaCodeInput,\n    colorAttempt: state.enigmaColorAttempt,\n    puzzleOrder: state.enigmaPuzzleOrder,\n    dragSlots: state.enigmaDragSlots,\n    rotationAngles: state.enigmaRotationAngles,\n  };\n}\n\nfunction applySolveEnigmaAction(action = {}) {\n  if (!state.activeEnigma?.enigma) return false;\n  const { enigma } = state.activeEnigma;\n  if (action.id && action.id !== enigma.id) return false;\n  const isSuccess = validateEnigmaAnswer(enigma, action.answer || getActiveEnigmaAnswer());\n\n  if (!isSuccess) {\n    failActiveEnigma();\n    if (enigma.type === 'colors') state.enigmaColorAttempt = [];\n    render();\n    return false;\n  }\n\n  solveActiveEnigma();\n  return true;\n}\n\nfunction submitEnigma() {\n  return dispatch(gameActions.solveEnigma(state.activeEnigma?.enigma?.id));\n}\n\nfunction pushEnigmaColor(colorValue) {\n  if (!state.activeEnigma?.enigma) return;\n  const expectedLength = state.activeEnigma.enigma.solutionColors?.length || 0;\n  const next = [...state.enigmaColorAttempt, colorValue].slice(0, expectedLength || state.enigmaColorAttempt.length + 1);\n  state.enigmaColorAttempt = next;\n\n  if (state.activeEnigma.enigma.type === 'simon') {\n    const solution = state.activeEnigma.enigma.solutionColors || [];\n    const failed = next.some((color, index) => color !== solution[index]);\n    if (failed) {\n      state.enigmaColorAttempt = [];\n      failActiveEnigma();\n      startSimonPlayback(state.activeEnigma.enigma);\n      render();\n      return;\n    }\n    if (next.length === solution.length) {\n      solveActiveEnigma();\n      return;\n    }\n  }\n\n  render();\n}\n\nfunction clickPuzzlePiece(index) {\n  if (state.enigmaPuzzleSelectedIndex === null) {\n    state.enigmaPuzzleSelectedIndex = index;\n    render();\n    return;\n  }\n\n  const next = [...state.enigmaPuzzleOrder];\n  [next[state.enigmaPuzzleSelectedIndex], next[index]] = [next[index], next[state.enigmaPuzzleSelectedIndex]];\n  state.enigmaPuzzleOrder = next;\n  state.enigmaPuzzleSelectedIndex = null;\n  render();\n\n  if (next.every((value, pieceIndex) => value === pieceIndex)) {\n    window.setTimeout(() => solveActiveEnigma(), 120);\n  }\n}\n\nfunction rotatePuzzlePiece(index) {\n  const next = [...state.enigmaRotationAngles];\n  next[index] = (((next[index] || 0) + 90) % 360);\n  state.enigmaRotationAngles = next;\n  render();\n\n  if (next.every((value) => value % 360 === 0)) {\n    window.setTimeout(() => solveActiveEnigma(), 120);\n  }\n}\n\nfunction moveDragPieceToSlot(pièce, slotIndex) {\n  if (pièce === null || pièce === undefined) return;\n  const bankWithoutPiece = state.enigmaDragBank.filter((entry) => entry !== pièce);\n  const nextSlots = [...state.enigmaDragSlots];\n  const previousSlotIndex = nextSlots.findIndex((entry) => entry === pièce);\n  if (previousSlotIndex >= 0) nextSlots[previousSlotIndex] = null;\n  const displacedPiece = nextSlots[slotIndex];\n  nextSlots[slotIndex] = pièce;\n\n  state.enigmaDragSlots = nextSlots;\n  state.enigmaDragBank = displacedPiece === null || displacedPiece === undefined ?\n     bankWithoutPiece\n    : [...bankWithoutPiece, displacedPiece];\n  render();\n\n  if (nextSlots.every((entry, index) => entry === index)) {\n    window.setTimeout(() => solveActiveEnigma(), 120);\n  }\n}\n\nfunction returnDragPieceToBank(slotIndex) {\n  const nextSlots = [...state.enigmaDragSlots];\n  const pièce = nextSlots[slotIndex];\n  if (pièce !== null && pièce !== undefined) {\n    nextSlots[slotIndex] = null;\n    state.enigmaDragSlots = nextSlots;\n    state.enigmaDragBank = [...state.enigmaDragBank, pièce];\n    render();\n  }\n}\n\n";

export const standaloneEnigmaRender = "function getEnigmaPrimaryButtonStyle() {\n  return 'color:#fff;background:linear-gradient(180deg,#4f8cff 0%,#2f6fe4 100%);border:1px solid rgba(255,255,255,.12);box-shadow:0 10px 24px rgba(47,111,228,.24)';\n}\n\nfunction getEnigmaSecondaryButtonStyle() {\n  return 'color:#dbeafe;background:rgba(15,23,42,.92);border:1px solid rgba(147,197,253,.28)';\n}\n\nfunction renderEnigmaSubmitButton(style) {\n  return '<button id=\"submit-enigma\" style=\"' + style + '\">Valider l\u2019\u00e9nigme</button>';\n}\n\nfunction getEnigmaGridMetrics(enigma) {\n  const rows = Number(enigma.gridRows) || 3;\n  const cols = Number(enigma.gridCols) || 3;\n  return {\n    rows,\n    cols,\n    pieceCount: rows * cols,\n  };\n}\n\nfunction renderEnigmaGrid(cols, content) {\n  return '<div class=\"enigma-grid\" style=\"grid-template-columns:repeat(' + cssNumber(cols, 3, 1, 24) + ', 1fr)\">' + content + '</div>';\n}\n\nfunction renderImageRequiredEnigma() {\n  return '<p class=\"small-note\">Ajoute une image dans l\u2019\u00e9diteur d\u2019\u00e9nigmes pour jouer cette \u00e9nigme.</p>';\n}\n\nfunction renderCodeEnigma(enigma) {\n  const codeSkin = enigma.codeSkin || 'safe-wheels';\n  const codeLength = Math.min(Math.max(4, String(enigma.solutionText || '').length || 4), 8);\n  const codeSlots = Array.from({ length: codeLength }, (_, index) => state.enigmaCodeInput[index] || '');\n  const slotInputStyle = 'width:54px;height:64px;text-align:center;font-size:24px;font-weight:900;border-radius:14px;border:1px solid rgba(255,255,255,.22);background:rgba(12,21,39,.9);color:white;outline:none';\n  const boxInputStyle = 'width:50px;height:50px;text-align:center;font-size:22px;font-weight:900;border-radius:8px;border:2px solid rgba(96,165,250,.75);background:rgba(12,21,39,.9);color:white;outline:none';\n  const primaryButtonStyle = getEnigmaPrimaryButtonStyle();\n  const keypadKeys = CODE_KEYPAD_KEYS;\n\n  if (codeSkin === 'safe-wheels') {\n    return '<div><label>Roulettes du coffre</label><div style=\"display:flex;gap:10px;flex-wrap:wrap;justify-content:center;margin-top:10px\">'\n      + codeSlots.map((char, index) => '<input data-code-index=\"' + safeDataAttr(index) + '\" data-code-length=\"' + safeDataAttr(codeLength) + '\" maxlength=\"1\" value=\"' + escapeAttr(char) + '\" style=\"' + slotInputStyle + ';background:linear-gradient(180deg, rgba(255,255,255,.18), rgba(255,255,255,.04))\" />').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (codeSkin === 'digicode') {\n    return '<div><label>Digicode</label><div style=\"display:flex;gap:8px;justify-content:center;margin:10px 0 14px\">'\n      + codeSlots.map((char) => '<span style=\"width:42px;height:46px;border-radius:10px;border:1px solid rgba(255,255,255,.22);display:grid;place-items:center;font-size:22px;font-weight:900;background:rgba(15,23,42,.68)\">' + safeHtml(char || '?') + '</span>').join('')\n      + '</div><div style=\"display:grid;grid-template-columns:repeat(3,64px);gap:10px;justify-content:center\">'\n      + keypadKeys.map((key) => '<button type=\"button\" data-code-key=\"' + safeDataAttr(key) + '\" data-code-length=\"' + safeDataAttr(codeLength) + '\" style=\"height:52px;font-size:20px;font-weight:900;color:#f8fbff;background:linear-gradient(180deg, rgba(59,130,246,.32), rgba(30,41,59,.96));border:1px solid rgba(147,197,253,.34);box-shadow:0 10px 22px rgba(15,23,42,.28)\">' + safeHtml(key) + '</button>').join('')\n      + '</div><div class=\"inventory-actions\"><button id=\"clear-code\" style=\"' + getEnigmaSecondaryButtonStyle() + '\">Effacer</button>' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (codeSkin === 'boxes') {\n    return '<div><label>Cases du code</label><div style=\"display:flex;gap:10px;flex-wrap:wrap;justify-content:center;margin-top:10px\">'\n      + codeSlots.map((char, index) => '<input data-code-index=\"' + safeDataAttr(index) + '\" data-code-length=\"' + safeDataAttr(codeLength) + '\" maxlength=\"1\" value=\"' + escapeAttr(char) + '\" style=\"' + boxInputStyle + '\" />').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (codeSkin === 'paper-strip') {\n    return '<div><label>Bande papier</label><input id=\"enigma-input\" value=\"' + escapeAttr(state.enigmaCodeInput) + '\" '\n      + 'style=\"width:100%;padding:12px 14px;border-radius:8px;border:1px solid rgba(148,163,184,.16);background:rgba(255,255,255,.92);color:#0f172a;outline:none;text-align:center;font-family:monospace;font-size:24px;font-weight:900;letter-spacing:8px;text-transform:uppercase\" />'\n      + '<div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  return '<div><label>Code</label><input id=\"enigma-input\" value=\"' + escapeAttr(state.enigmaCodeInput) + '\" '\n    + 'style=\"width:100%;padding:12px 14px;border-radius:14px;border:1px solid rgba(148,163,184,.16);background:rgba(12,21,39,.9);color:white;outline:none\" />'\n    + '<div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n}\n\nfunction renderColorEnigma() {\n  return '<div><label>Suite en cours</label><div class=\"color-attempt-row\">'\n    + (state.enigmaColorAttempt.length\n      ? state.enigmaColorAttempt.map((color) => '<span class=\"color-chip\" style=\"background:' + safeCssColor(color, 'transparent') + '\"></span>').join('')\n      : '<span class=\"small-note\">Aucune couleur choisie.</span>')\n    + '</div><div class=\"color-picker-grid\">'\n    + PREVIEW_COLOR_OPTIONS.map(([value, label]) => '<button type=\"button\" class=\"color-picker-button\" data-enigma-color=\"' + safeDataAttr(value) + '\" title=\"' + escapeAttr(label) + '\" style=\"background:' + safeCssColor(value, 'transparent') + '\"></button>').join('')\n    + '</div><div class=\"panel-head\"><button id=\"clear-colors\" class=\"secondary-button\">Effacer la suite</button><button id=\"submit-enigma\">Valider l\u2019\u00e9nigme</button></div></div>';\n}\n\nfunction renderMiscEnigma(enigma) {\n  const miscMode = enigma.miscMode || 'free-answer';\n  const primaryButtonStyle = getEnigmaPrimaryButtonStyle();\n  const secondaryButtonStyle = getEnigmaSecondaryButtonStyle();\n\n  if (miscMode === 'multiple-choice') {\n    return '<div><label>Choisis une r\u00e9ponse</label><div style=\"display:grid;gap:10px;margin-top:10px\">'\n      + (enigma.miscChoices || []).map((choice) => '<button type=\"button\" data-misc-choice=\"' + safeDataAttr(choice) + '\" style=\"' + (state.enigmaCodeInput === choice ? primaryButtonStyle : secondaryButtonStyle) + '\">' + safeHtml(choice) + '</button>').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (miscMode === 'true-false') {\n    return '<div><label>Choisis une r\u00e9ponse</label><div style=\"display:flex;gap:10px;flex-wrap:wrap;margin-top:10px\">'\n      + ['vrai', 'faux'].map((choice) => '<button type=\"button\" data-misc-choice=\"' + safeDataAttr(choice) + '\" style=\"' + (state.enigmaCodeInput === choice ? primaryButtonStyle : secondaryButtonStyle) + '\">' + (choice === 'vrai' ? 'Vrai' : 'Faux') + '</button>').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (miscMode === 'ordering') {\n    const current = parseJsonValue(state.enigmaCodeInput, []);\n    return '<div><label>Remets dans l\u2019ordre</label><div style=\"display:grid;gap:10px;margin-top:10px\">'\n      + '<div class=\"color-attempt-row\">'\n      + (current.length ? current.map((choice, index) => '<button type=\"button\" data-misc-order-remove=\"' + safeDataAttr(index) + '\" style=\"' + secondaryButtonStyle + '\">' + (index + 1) + '. ' + safeHtml(choice) + '</button>').join('') : '<span class=\"small-note\">Clique les \u00e9l\u00e9ments dans le bon ordre.</span>')\n      + '</div>'\n      + (enigma.miscChoices || []).filter((choice) => !current.includes(choice)).map((choice) => '<button type=\"button\" data-misc-order=\"' + safeDataAttr(choice) + '\" style=\"' + secondaryButtonStyle + '\">' + safeHtml(choice) + '</button>').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (miscMode === 'matching') {\n    const answers = parseJsonValue(state.enigmaCodeInput, {});\n    return '<div><label>Associe les paires</label><div style=\"display:grid;gap:10px;margin-top:10px\">'\n      + (enigma.miscPairs || []).map((pair) => '<div style=\"display:grid;grid-template-columns:1fr 1fr;gap:10px;align-items:center\"><strong>' + safeHtml(pair.left || '') + '</strong><select data-misc-match-left=\"' + safeDataAttr(pair.left || '') + '\"><option value=\"\">Choisir</option>'\n        + (enigma.miscPairs || []).map((entry) => '<option value=\"' + escapeAttr(entry.right || '') + '\"' + (answers[pair.left] === entry.right ? ' selected' : '') + '>' + safeHtml(entry.right || '') + '</option>').join('')\n        + '</select></div>').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (miscMode === 'numeric-range' || miscMode === 'exact-number') {\n    return '<div><label>' + (miscMode === 'exact-number' ? 'Nombre exact' : 'Nombre') + '</label><input id=\"enigma-input\" type=\"number\" value=\"' + escapeAttr(state.enigmaCodeInput) + '\" '\n      + 'style=\"width:100%;padding:12px 14px;border-radius:14px;border:1px solid rgba(148,163,184,.16);background:rgba(12,21,39,.9);color:white;outline:none\" />'\n      + '<p class=\"small-note\">' + (miscMode === 'exact-number' ? 'La r\u00e9ponse doit correspondre au nombre attendu.' : 'La r\u00e9ponse doit \u00eatre comprise entre ' + safeHtml(enigma.miscMin ?? '') + ' et ' + safeHtml(enigma.miscMax ?? '') + '.') + '</p>'\n      + '<div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (miscMode === 'item-select') {\n    return '<div><label>Choisis l\u2019objet</label><div style=\"display:grid;gap:10px;margin-top:10px\">'\n      + (project.items || []).map((item) => '<button type=\"button\" data-misc-choice=\"' + safeDataAttr(item.id) + '\" style=\"' + (state.enigmaCodeInput === item.id ? primaryButtonStyle : secondaryButtonStyle) + '\">' + safeHtml(item.name || 'Objet') + '</button>').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  if (miscMode === 'multi-select') {\n    const current = parseJsonValue(state.enigmaCodeInput, []);\n    return '<div><label>Selectionne toutes les bonnes r\u00e9ponses</label><div style=\"display:grid;gap:10px;margin-top:10px\">'\n      + (enigma.miscChoices || []).map((choice) => '<button type=\"button\" data-misc-toggle=\"' + safeDataAttr(choice) + '\" style=\"' + (current.includes(choice) ? primaryButtonStyle : secondaryButtonStyle) + '\">' + safeHtml(choice) + '</button>').join('')\n      + '</div><div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n  }\n\n  return '<div><label>' + (miscMode === 'fill-blank' ? 'Mot manquant' : 'R\u00e9ponse') + '</label><input id=\"enigma-input\" value=\"' + escapeAttr(state.enigmaCodeInput) + '\" placeholder=\"\u00c9cris ta r\u00e9ponse...\" '\n    + 'style=\"width:100%;padding:12px 14px;border-radius:14px;border:1px solid rgba(148,163,184,.16);background:rgba(12,21,39,.9);color:white;outline:none\" />'\n    + '<p class=\"small-note\">La r\u00e9ponse est accept\u00e9e m\u00eame avec des majuscules diff\u00e9rentes ou des mots en plus.</p>'\n    + '<div class=\"inventory-actions\">' + renderEnigmaSubmitButton(primaryButtonStyle) + '</div></div>';\n}\n\nfunction renderSimonEnigma(enigma) {\n  return '<div><p class=\"small-note\">' + (state.simonPlayerTurn ? '\u00c0 toi de rejouer la sequence.' : 'Observe la sequence.') + '</p>'\n    + '<div class=\"color-picker-grid simon-grid\">'\n    + PREVIEW_COLOR_OPTIONS.slice(0, 4).map(([value, label], index) => {\n      const solutionColor = (enigma.solutionColors || [])[state.simonPlaybackIndex];\n      const lit = solutionColor === value ? ' active' : '';\n      return '<button type=\"button\" class=\"color-picker-button simon-pad' + lit + '\" data-simon-color=\"' + safeDataAttr(value) + '\" title=\"' + escapeAttr(label) + '\" style=\"background:' + safeCssColor(value, 'transparent') + '\"' + (state.simonPlayerTurn ? '' : ' disabled') + '>' + (index + 1) + '</button>';\n    }).join('')\n    + '</div><div class=\"color-attempt-row\" style=\"margin-top:14px\">'\n    + state.enigmaColorAttempt.map((color) => '<span class=\"color-chip\" style=\"background:' + safeCssColor(color, 'transparent') + '\"></span>').join('')\n    + '</div><div class=\"inventory-actions\"><button id=\"replay-simon\" class=\"secondary-button\">Rejouer la sequence</button></div></div>';\n}\n\nfunction renderPuzzleEnigma(enigma, enigmaImageUrl) {\n  const { rows, cols } = getEnigmaGridMetrics(enigma);\n  return '<div><p class=\"small-note\">Clique une pi\u00e8ce, puis une deuxi\u00e8me pour les \u00e9changer.</p>'\n    + renderEnigmaGrid(cols, state.enigmaPuzzleOrder.map((pieceIndex, index) => '<button type=\"button\" data-puzzle-index=\"' + safeDataAttr(index) + '\" class=\"puzzle-piece'\n      + (state.enigmaPuzzleSelectedIndex === index ? ' selected' : '') + '\" style=\"' + makePieceStyle(enigmaImageUrl, rows, cols, pieceIndex) + '\"></button>').join(''))\n    + '</div>';\n}\n\nfunction renderRotationEnigma(enigma, enigmaImageUrl) {\n  const { rows, cols, pieceCount } = getEnigmaGridMetrics(enigma);\n  return '<div><p class=\"small-note\">Clique sur chaque pi\u00e8ce pour la remettre \u00e0 l\u2019endroit.</p>'\n    + renderEnigmaGrid(cols, Array.from({ length: pieceCount }, (_, index) => '<button type=\"button\" data-rotation-index=\"' + safeDataAttr(index) + '\" class=\"puzzle-piece\" style=\"'\n      + makePieceStyle(enigmaImageUrl, rows, cols, index, state.enigmaRotationAngles[index] || 0) + '\"></button>').join(''))\n    + '</div>';\n}\n\nfunction renderDragdropEnigma(enigma, enigmaImageUrl) {\n  const { rows, cols } = getEnigmaGridMetrics(enigma);\n  return '<div><p class=\"small-note\">Glisse les pi\u00e8ces vers la bonne case. Clique une case remplie pour renvoyer sa pi\u00e8ce dans la r\u00e9serve.</p>'\n    + '<div class=\"dragdrop-layout\"><div><h3>Plateau</h3>'\n    + renderEnigmaGrid(cols, state.enigmaDragSlots.map((pieceIndex, slotIndex) => '<button type=\"button\" data-slot-index=\"' + safeDataAttr(slotIndex) + '\" class=\"puzzle-slot\">'\n      + (pieceIndex !== null && pieceIndex !== undefined\n        ? '<span class=\"puzzle-piece static\" style=\"' + makePieceStyle(enigmaImageUrl, rows, cols, pieceIndex) + '\"></span>'\n        : '<span class=\"slot-index\">' + (slotIndex + 1) + '</span>') + '</button>').join(''))\n    + '</div><div><h3>Pi\u00e8ces</h3><div class=\"bank-grid\">'\n    + state.enigmaDragBank.map((pieceIndex) => '<button type=\"button\" data-bank-piece=\"' + safeDataAttr(pieceIndex) + '\" class=\"puzzle-piece\" draggable=\"true\" style=\"'\n      + makePieceStyle(enigmaImageUrl, rows, cols, pieceIndex) + '\"></button>').join('')\n    + '</div></div></div></div>';\n}\n\nfunction renderEnigmaShell(enigma, body) {\n  const enigmaPopupBackgroundUrl = resolveAssetUrl(enigma.popupBackgroundId, enigma.popupBackgroundData);\n  const overlayGradient = POPUP_OVERLAY_GRADIENTS[enigma.popupBackgroundOverlay] || POPUP_OVERLAY_GRADIENTS.dark;\n  const overlayStyle = enigmaPopupBackgroundUrl\n    ? ' style=\"background-image:' + overlayGradient + ', ' + cssMediaUrl(enigmaPopupBackgroundUrl, 'image') + ';background-size:' + cssNumber((Number(enigma.popupBackgroundZoom) || 1) * 100, 100, 1, 500) + '%;background-position:' + safeStylePercent(enigma.popupBackgroundX, 50) + ' ' + safeStylePercent(enigma.popupBackgroundY, 50) + ';background-repeat:no-repeat\"'\n    : '';\n\n  return '<div class=\"overlay\" id=\"enigma-overlay\"><div class=\"overlay-card\"' + overlayStyle + '><div class=\"panel-head\"><div><h2 style=\"margin:0\">'\n    + safeHtml(enigma.name || '\u00e9nigme') + '</h2><p class=\"small-note\" style=\"margin:6px 0 0\">'\n    + safeHtml(enigma.question || '') + '</p></div><button id=\"close-enigma\" class=\"danger-button\">Fermer</button></div>'\n    + body + '</div></div>';\n}\n\nfunction renderEnigma(enigma) {\n  if (!enigma) return '';\n\n  const enigmaImageUrl = resolveAssetUrl(enigma.imageId, enigma.imageData);\n  let body = '';\n\n  if (enigma.type === 'code') body = renderCodeEnigma(enigma);\n  if (enigma.type === 'colors') body = renderColorEnigma(enigma);\n  if (enigma.type === 'misc') body = renderMiscEnigma(enigma);\n  if (enigma.type === 'simon') body = renderSimonEnigma(enigma);\n  if (enigma.type === 'puzzle' && enigmaImageUrl) body = renderPuzzleEnigma(enigma, enigmaImageUrl);\n  if (enigma.type === 'rotation' && enigmaImageUrl) body = renderRotationEnigma(enigma, enigmaImageUrl);\n  if (enigma.type === 'dragdrop' && enigmaImageUrl) body = renderDragdropEnigma(enigma, enigmaImageUrl);\n  if (usesImage(enigma.type) && !enigmaImageUrl) body = renderImageRequiredEnigma();\n\n  return renderEnigmaShell(enigma, body);\n}\n\n";
